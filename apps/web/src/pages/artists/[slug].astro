---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PagePlaceholder from '../../components/ui/PagePlaceholder.astro';

export async function getStaticPaths() {
  // Wired to Sanity in Phase 2 (artistBySlugQuery per artist).
  return [];
}
---

<BaseLayout title="Artist" description="Artist bio, discography, and embeds.">
  <PagePlaceholder heading="Artist" />
</BaseLayout>
